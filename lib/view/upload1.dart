import 'package:flutter/material.dart';

class Upload1 extends StatelessWidget {
  const Upload1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Replaced upload page'),
      ),
    );
  }
}
